# Arabic and English Localization

Arabic and English are first-class.

Language resolution:
1. Explicit account preference.
2. Explicit session preference.
3. Browser language.
4. Platform default.

Never override an explicit choice automatically.

All user-facing strings are externalized. Dates, numbers, currency, pluralization, typography, and layout direction are locale-aware.

RTL is a first-class layout direction.
