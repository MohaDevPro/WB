# Design System

Define tokens for color, typography, spacing, radius, elevation, borders, icons, motion, responsive breakpoints, and semantic states.

Reusable components include navigation, hero, cards, profiles, experts, services, events, partners, trust badges, ratings, filters, search, forms, modals, drawers, notifications, loading, empty, and error states.

Components must support RTL and LTR without duplicated implementations.
