# OpenCode Instructions

Follow AGENTS.md and the project instruction hierarchy.

Use focused changes, preserve domain boundaries, run relevant tests, and report failures rather than masking them.

Do not introduce dependencies or infrastructure changes without a clear reason and documentation impact assessment.
