# CI/CD

The pipeline should include dependency installation, formatting/lint, type checks, unit tests, integration tests where practical, security scanning, dependency/license checks, build, protected-branch E2E tests, and artifact generation.

Deployment requires successful gates and appropriate approvals.
