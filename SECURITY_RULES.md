# Security Rules

Use least privilege, secure authentication, authorization checks, input validation, output encoding, rate limiting, secure headers, CSRF protections where applicable, SSRF controls, safe file handling, secrets management, dependency scanning, audit logging, and secure defaults.

Never commit production secrets.

Security-sensitive changes require review and automated checks.
