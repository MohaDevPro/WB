# Testing Strategy

Use unit tests for deterministic domain logic, integration tests for persistence and service boundaries, API tests for contracts, E2E tests for critical journeys, security tests for security controls, accessibility tests for user-facing flows, and performance tests for critical paths.

Payment, identity, trust, authorization, and settlement workflows require stronger coverage.
