# AI Architecture

AI is a platform capability rather than a single chatbot.

The architecture includes an AI Gateway, model adapters, prompt/context management, retrieval, tool execution, agent orchestration, evaluation, observability, policy enforcement, and auditability.

The AI Gateway is provider-agnostic. AI never bypasses authorization; every tool action executes under a defined capability and policy.
