# Engineering Rules

Use TypeScript strictness, explicit types at domain boundaries, cohesive modules, clear naming, and appropriate dependency inversion.

Validate external input at boundaries. Keep business rules independent from transport and persistence details.

Avoid circular dependencies and hidden global state. Isolate nondeterministic AI behaviour behind interfaces.

Document architectural decisions that are difficult to reverse.
